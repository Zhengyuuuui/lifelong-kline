import React, { useState } from 'react';
import { Lightbulb, X } from 'lucide-react';

interface ModuleHelperTagProps {
  text: string;
}

export const ModuleHelperTag: React.FC<ModuleHelperTagProps> = ({ text }) => {
  return null;
};
